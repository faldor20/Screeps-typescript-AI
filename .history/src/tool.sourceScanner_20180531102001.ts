var sourceScanner = {

    /** @param {Room} room **/
    scan: function (room:Room) {
     let sources=   room.find(FIND_SOURCES);
        sources.forEach(source:source => {
            source
        }); 
    }
};
module.exports= toolsourceScanner;